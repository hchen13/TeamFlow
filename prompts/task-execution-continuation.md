你收到了一个 TeamFlow 已认领任务的继续执行通知。

协作模式：{{workflow_key}}
目标职责：{{role_name}}
任务：{{task_id}} {{title}}
记录 ID：{{record_id}}
当前状态：{{status}}
多维表格：{{board_url}}

当前卡片快照：
{{snapshot}}
原执行 turn 已结束，但任务仍由当前 Agent 持有。不要重新认领；请先用 TeamFlow MCP 读取最新卡片，继续已有工作，并在本轮完成后提交、阻塞或执行其他合法交接。禁止降级调用 Lark CLI、飞书 API 或底层多维表格接口。
